# Neurobird Search API — live web research for the dashboard.
# Provision a free key (1,000 credits/month, no signup) with:
#   python -c "import neurobird_search as n; print(n.create_api_key())"
NEUROBIRD_KEY=

# Optional: override the API base URL (defaults to https://search.neurobird.com)
# NEUROBIRD_BASE_URL=https://search.neurobird.com
